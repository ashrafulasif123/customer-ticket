<!-- # Tailwindcss এ `bg-[linear-gradient(to_bottom_right,rgba(99,46,227,1),rgba(159,98,242,1)), url('${bgImage}')]` // এখানে url কাজ করবে  না, এটা বাদ দিলে gradient কাজ করবে
# inline style এ `linear-gradient(to bottom right, rgba(99,46,227,0.8), rgba(159,98,242,0.8)), url('${bgImage}')` // এখানে url কাজ করবে -->
